import { Logger } from '@ngtools/logger';
export declare function buildSchema(inFile: string, _logger: Logger): string;
export declare function build(args: string[], _opts: any, logger: Logger): void;
